<% @order.line_items.each do |line_item| %>
  {
    id: '<%= j (line_item.sku.present? ? line_item.sku : line_item.variant_id.to_s) %>',
    quantity: <%= line_item.quantity %>,
    item_price: '<%= line_item.price.to_f %>',
  },
<% end %>
