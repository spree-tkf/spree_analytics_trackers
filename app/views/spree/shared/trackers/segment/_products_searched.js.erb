<% if tracker = Spree::Tracker.current(:segment) && params.key?(:keywords) %>
    <script>
      if (typeof analytics !== 'undefined') {

        analytics.track('Products Searched', {
          query: '<%= params[:keywords] %>',
        });

        analytics.page("Product Searched");

      }
    </script>
<% end %>
