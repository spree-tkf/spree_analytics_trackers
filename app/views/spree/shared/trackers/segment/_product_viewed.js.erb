<% if tracker = Spree::Tracker.current(:segment) %>
  <script>
    if (typeof analytics !== 'undefined') {
      
      analytics.track('product', <%= product_for_segment(@product, image: @product.images.first) %>);

    }
  </script>
<% end %>
